---
import { getFilteredProducts } from '../services/notion';
import { config } from '../config/filter';
import faCss from '@fortawesome/fontawesome-free/css/all.min.css?url';

const products = await getFilteredProducts();
---

<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="utf-8" />
  <meta content="width=device-width, initial-scale=1.0" name="viewport" />
  <title>Catálogo - {config.clientFilter}</title>
  <link href="https://fonts.googleapis.com" rel="preconnect" />
  <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <link
    href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
    rel="stylesheet" />
  <link rel="stylesheet" href={faCss} />
  <script is:inline src="https://cdn.tailwindcss.com?plugins=forms,typography"></script>
    <style>
      .material-symbols-outlined {
        font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
      }

      body {
        font-family: 'Inter', sans-serif;
      }

      .qty-input::-webkit-inner-spin-button,
      .qty-input::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
      }

      .qty-input {
        -moz-appearance: textfield;
      }
    </style>
</head>

<body class="bg-gradient-to-br from-white via-indigo-50 to-indigo-100 text-slate-900">
  <div class="max-w-7xl mx-auto px-6 py-12 md:py-20">
    <header class="flex flex-col md:flex-row md:items-start justify-between mb-16 gap-8">
      <div class="max-w-xl">
        <h1 class="text-5xl md:text-5xl font-bold text-indigo-600 mb-8 tracking-tight">
          <span class="opacity-50 font-thin">Catálogo</span>&nbsp;{config.clientFilter}
        </h1>
        <p class="text-slate-600 leading-relaxed text-lg">
          Hemos seleccionado este conjunto de productos con cuidado para tu marca <strong>{config.clientFilter}</strong>. Creemos que representan la
          esencia de tu marca e impactarán a tu audiencia. Todos los productos son personalizables, lo que te permite
          adaptarlos a tu estilo y necesidades específicas.
        </p>
      </div>
      <div class="flex gap-4">
        <button
          id="catalog-pdf-btn"
          data-client={config.clientFilter}
          class="px-8 py-3 rounded-full border-2 border-indigo-600 text-indigo-600 font-medium hover:bg-rose-600 hover:text-white hover:border-rose-600 transition-all duration-300 flex items-center gap-2">
          <i class="fa-solid fa-download"></i>
          Descargar
        </button>
      </div>
    </header>

    {products.length > 0 ? (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {products.map((product) => {
          const rawPrice = typeof product.price === 'number' ? product.price : Number(product.price) || 0;
          const displayPrice = `€${rawPrice}`;
          return (
            <div
              class="group relative bg-white border-2 border-indigo-600 rounded-3xl p-8 flex flex-col transition-transform duration-300 hover:-translate-y-2"
              data-product-id={product.id}
              data-product-name={product.name || 'Producto'}
              data-product-price={rawPrice}
            >
              <div class="flex justify-between items-start mb-8 gap-3">
                <span class="px-4 py-1.5 rounded-full border border-slate-200 text-sm font-medium text-slate-600">
                  {product.category || 'Original'}
                </span>
                <div class="flex items-center gap-2 text-slate-700" aria-label="Control cantidad">
                  <button
                    class="w-8 h-8 rounded-full border border-indigo-200 text-indigo-700 bg-indigo-50 flex items-center justify-center hover:bg-indigo-600 hover:border-indigo-600 hover:text-white transition disabled:opacity-40 disabled:cursor-not-allowed"
                    type="button"
                    data-action="decrement"
                    aria-label={`Quitar ${product.name || 'producto'}`}
                  >
                    −
                  </button>
                  <input
                    type="number"
                    min="0"
                    value="0"
                    inputmode="numeric"
                    class="qty-input w-20 rounded-md border border-slate-200 px-3 py-1.5 text-center font-semibold focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                    data-role="count"
                    aria-label={`Cantidad de ${product.name || 'producto'}`}
                  />
                  <button
                    class="w-8 h-8 rounded-full border border-indigo-200 text-indigo-700 bg-indigo-50 flex items-center justify-center hover:bg-indigo-600 hover:border-indigo-600 hover:text-white transition"
                    type="button"
                    data-action="increment"
                    aria-label={`Añadir ${product.name || 'producto'}`}
                  >
                    +
                  </button>
                </div>
              </div>
              <div class="aspect-square w-full rounded-2xl bg-gradient-to-br from-slate-100 to-slate-200 flex items-center justify-center mb-8 overflow-hidden">
                {product.image ? (
                  <img
                    alt={product.name || 'Producto'}
                    class="w-full h-full object-cover drop-shadow-2xl group-hover:scale-110 transition-transform duration-500"
                    src={product.image}
                  />
                ) : (
                  <span class="material-symbols-outlined text-6xl text-slate-300">image</span>
                )}
              </div>
              <div class="mt-auto">
                <div class="flex justify-between items-baseline mb-1">
                  <h3 class="text-xl font-bold text-slate-900">{product.name || 'Producto'}</h3>
                  <span class="text-xl font-bold text-slate-900">{displayPrice}</span>
                </div>
                <p class="text-slate-500">{product.description || 'Personalizable y listo para tu marca.'}</p>
              </div>
            </div>
          );
        })}
      </div>
    ) : (
      <div class="text-center py-20">
        <span class="material-symbols-outlined text-6xl text-slate-300 mb-4">inventory_2</span>
        <p class="text-slate-500 text-lg">
          Aún no hemos podido seleccionar productos para <strong>{config.clientFilter}</strong>.
        </p>
      </div>
    )}

    <section id="presupuesto" class="mt-16 bg-white border-2 border-indigo-600 rounded-3xl p-8 shadow-sm">
      <div class="flex items-center justify-between mb-6">
        <div>
          <p class="text-sm uppercase tracking-wide text-indigo-600 font-semibold">Resumen</p>
          <h2 class="text-3xl font-bold text-slate-900">Presupuesto</h2>
        </div>
        <div class="text-right">
          <p class="text-sm text-slate-500">Cliente</p>
          <p class="text-lg font-semibold text-slate-900">{config.clientFilter}</p>
        </div>
      </div>

      <div id="presupuesto-items" class="space-y-4 text-slate-800">
        <p class="text-slate-500" data-empty>Sin productos añadidos.</p>
      </div>

      <div class="mt-6 pt-6 border-t border-slate-200 flex items-center justify-between">
        <span class="text-lg font-semibold text-slate-700">Total</span>
        <span id="presupuesto-total" class="text-2xl font-bold text-indigo-700">€0.00</span>
      </div>
    </section>

    <footer class="mt-24 pt-12 border-t border-slate-200 flex flex-col md:flex-row justify-between items-center gap-6">
      <p class="text-slate-500 text-sm">© 2026 MrPrinteto. All rights reserved.</p>
      <div class="flex gap-4 text-slate-500 text-xl">
        <a class="hover:text-slate-700 transition" href="https://mrprinteto.com" aria-label="Web mrprinteto.com" target="_blank" rel="noreferrer">
          <i class="fa-solid fa-globe"></i>
        </a>
        <a class="hover:text-slate-700 transition" href="https://instagram.com/mrprinteto" aria-label="Instagram @mrprinteto" target="_blank" rel="noreferrer">
          <i class="fa-brands fa-instagram"></i>
        </a>
        <a class="hover:text-slate-700 transition" href="https://www.tiktok.com/@mrprinteto" aria-label="TikTok @mrprinteto" target="_blank" rel="noreferrer">
          <i class="fa-brands fa-tiktok"></i>
        </a>
        <a class="hover:text-slate-700 transition" href="https://youtube.com/@mrprinteto" aria-label="YouTube @mrprinteto" target="_blank" rel="noreferrer">
          <i class="fa-brands fa-youtube"></i>
        </a>
        <a class="hover:text-slate-700 transition" href="https://www.threads.net/@mrprinteto" aria-label="Threads @mrprinteto" target="_blank" rel="noreferrer">
          <i class="fa-brands fa-threads"></i>
        </a>
        <a class="hover:text-slate-700 transition" href="https://discord.gg/g9c7Nhrkea" aria-label="Discord" target="_blank" rel="noreferrer">
          <i class="fa-brands fa-discord"></i>
        </a>
      </div>
    </footer>
  </div>

  <script type="module" src="/src/scripts/catalog-pdf.ts"></script>
  <script type="module" src="/src/scripts/presupuesto.ts"></script>

</body>

</html>
